<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.control.Button?>
<?import javafx.scene.control.Label?>
<?import javafx.scene.control.TextField?>
<?import javafx.scene.layout.AnchorPane?>
<?import javafx.scene.layout.VBox?>

<AnchorPane xmlns="http://javafx.com/javafx"
            xmlns:fx="http://javafx.com/fxml"
            fx:controller="controllers.Ticketcontroller"
            prefHeight="700.0" prefWidth="700.0"
            style="-fx-background-color: white;">

    <children>
        <VBox layoutX="40.0" layoutY="30.0" spacing="15.0"
              prefWidth="620.0"
              style="
                  -fx-background-color: white;
                  -fx-padding: 25;
                  -fx-border-color: orange;
                  -fx-border-width: 2;
                  -fx-border-radius: 8;
                  -fx-background-radius: 8;
              ">

            <!-- Titre -->
            <Label text="🎫 Ajouter un Ticket"
                   style="-fx-font-size: 22px; -fx-font-weight: bold; -fx-text-fill: orange;" />

            <!-- Champs de saisie -->
            <Label text="Nom :" />
            <TextField fx:id="tfNom" promptText="Entrez le nom" />

            <Label text="Prénom :" />
            <TextField fx:id="tfPrenom" promptText="Entrez le prénom" />

            <Label text="Numéro de Téléphone :" />
            <TextField fx:id="tfTel" promptText="Ex: +216 99 999 999" />

            <Label text="Email :" />
            <TextField fx:id="tfEmail" promptText="exemple@domaine.com" />

            <Label text="Prix :" />
            <TextField fx:id="tfPrix" promptText="Ex: 25.000" />

            <Label text="ID Réservation :" />
            <TextField fx:id="tfReservationId" promptText="ID de la réservation" editable="true" />

            <!-- Bouton Ajouter -->
            <Button fx:id="ajouterBtn" text="✅ Ajouter le Ticket" onAction="#handleAjouter"
                    style="-fx-background-color: orange; -fx-text-fill: white; -fx-font-weight: bold; -fx-background-radius: 5;" />

            <!-- Bouton Afficher Tickets -->
            <Button fx:id="btnAfficher" text="📋 Afficher Tickets" onAction="#handleAfficherTickets"
                    style="-fx-background-color: #ffeb3b; -fx-text-fill: white; -fx-font-weight: bold; -fx-background-radius: 5;" />

        </VBox>
    </children>
</AnchorPane>
